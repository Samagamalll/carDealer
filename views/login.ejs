<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles_login.css">
    <script src="/js/loginValidate.js"></script>
    <title>Login</title>
    <style>
        .error {
            color: red;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h1>Login</h1>
        <form action="/login" name="Form" method="POST" onsubmit="return validateForm()">
            <a id="gobackbtn" href="/">Go Back</a>
            <div class="form-group">
                <input type="text" id="email" name="email"  placeholder="email">
                <div class="error" id="emailErr"></div>
            </div>
            <div class="form-group">
                <input type="password" id="password" name="password" placeholder="password">
                <div class="error" id="passwordErr"></div>
            </div>
            <% if (error) { %>
                <div class="error"> <%= error %></div>
            <% } %>
            <button type="submit" name="log" class="btn-submit">Login</button>
            <div class="form-group">
                Dont't have an account yet?<a href="/register"> Register for free</a>
            </div>
        </form>
    </div>
</body>
</html>

